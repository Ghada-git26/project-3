import React from "react";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <nav className="Navbar">
      <div>
        <Link exact to="/">
        </Link>
      </div>

    </nav>
  );
};

export default Home;